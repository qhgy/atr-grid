# ATR 网格交易项目 — 重构交班日志

> 最新更新：2026-04-23  
> 状态：**自动化全部上线，三 ETF Dashboard 运行中**

---

## 📋 工作记录（2026-04-23）

| # | 任务 | 结果 |
|---|------|------|
| 1 | 修复雪球数据通路 | cookie 路径 + 到期检查，`数据：api` 正常 |
| 2 | GitHub 仓库 + Pages | `qhgy/atr-grid` 公开，Pages 开通 |
| 3 | GitHub Actions 自动化 | 工作日 09:07 更新 Dashboard，18s 完成 |
| 4 | `multi` 命令 | 多 ETF 汇总 Dashboard，含汇总表格 + 各标的卡片 |
| 5 | Server酱微信推送 | 临近档位自动推，key 存 GitHub Secret |
| 6 | 历史快照 | 每日存 `snapshots/YYYY-MM-DD.html`，顶部日期选择器 |
| 7 | 默认持仓 2000 股 | 每档 200 股，"持仓不足"提示消失 |
| 8 | 交班手册小白指南 | cookie 更新 SOP、Actions 速查 |

**当前功能**：`plan` 单标的 · `multi` 多标的汇总 · `replay` 历史回放  
**监控标的**：SH515880 · SH513500 · SZ159915  
**Pages**：https://qhgy.github.io/atr-grid/

⚠️ **xq_a_token 还剩 7 天到期（2026-05-01）**，见交班手册第四节 SOP

---

## TL;DR（30 秒摘要）

**2026-04-23（今日）**：

1. 修复 `cli.py` 两处 bug（`_maybe_notify` 双 print / summary 行 onclick 丢失）
2. 上线 **`multi` 命令**：一次生成多个 ETF 的汇总 Dashboard
3. 接入 **Server酱微信推送**（临近档位时自动推送，key 存 GitHub Secret）
4. **历史快照**：每次运行自动保存 `output/snapshots/YYYY-MM-DD.html`，Dashboard 顶部有日期选择器
5. **GitHub Actions** 改为工作日北京时间 09:07 执行，运行 `multi SH515880 SH513500 SZ159915 --notify`
6. **GitHub Pages** 实时更新：https://qhgy.github.io/atr-grid/

2026-04-23（上午）：修复雪球数据通路（pysnowball + 新 cookie），添加 cookie 到期提示机制，推送到 `qhgy/atr-grid`，开通 GitHub Pages。

2026-04-17：把散落在 6 个文件中的 20+ 个魔法数字收进 `config.py`；修了一个会让脏数据伪装成 0 价格的 bug；消除了 engine.py 中 4 处重复的 40+ 字段 GridPlan 构造；replay 从"每天重算全部指标"优化为"算一次切片取用"。行为不变，数值一致。

## ⚠️ 紧急：xq_a_token 将于 2026-05-01 到期（还剩 7 天）

需要在浏览器重新导出 cookie，然后：
1. 覆盖 `D:\000000znb\1080\x1080x_attachments\xueqiu.com_cookies.txt`
2. 更新 GitHub Secret：`Get-Content <cookie文件> | gh secret set XUEQIUTOKEN --repo qhgy/atr-grid`

---

## 一、今日新增功能

### 1. `multi` 命令

```bash
uv run python -m atr_grid multi SH515880 SH513500 SZ159915
```

输出：
- 每个 ETF 的完整分析摘要（控制台）
- `output/atr_grid.html`：多标的汇总 Dashboard（含顶部总览表格 + 各 ETF 详细卡片）
- `output/snapshots/YYYY-MM-DD.html`：带日期的历史快照

### 2. Server酱微信通知

```bash
# 临近档位时推送
uv run python -m atr_grid multi SH515880 --notify

# 无条件推送（调试用）
uv run python -m atr_grid plan SH515880 --no-save --notify-always
```

环境变量：`SERVERCHAN_KEY=SCT341532T...`（已存 GitHub Secret）

触发条件（`should_notify`）：当前价距最近档位 ≤ 0.5%

### 3. 历史快照 + 日期选择器

每次运行 `multi` 自动保存 `output/snapshots/YYYY-MM-DD.html`，
主 Dashboard 顶部有下拉选择器，可以跳回任意历史日期查看。

### 4. GitHub Actions

- **触发**：工作日 09:07（北京时间）+ 手动触发
- **执行**：`multi SH515880 SH513500 SZ159915 --notify`
- **产物**：更新 `docs/index.html` + `docs/snapshots/今日.html` → 推送到仓库

---

## 二、已有功能（不变）

| 命令 | 用途 |
|------|------|
| `plan SH515880 --no-save` | 单 ETF 快速查看（不写文件） |
| `plan SH515880` | 生成 JSON + Markdown 报告 |
| `replay SH515880 --lookback 60` | 回放最近 60 个交易日 |
| `multi SH515880 SH513500 SZ159915` | 多标的汇总 Dashboard |

---

## 三、验证结果

### 单元测试（67 项全绿）

```
$ uv run python -m pytest tests/atr_grid/ -q
67 passed in 0.64s
```

### GitHub Actions

最近一次运行：`24826371001`，耗时 18s，状态 ✅

---

## 一、改动清单（what + why）

### 新增

| 文件 | 说明 | 为什么 |
|------|------|--------|
| `config.py` | `GridConfig` dataclass，集中管理全部策略参数 | 原来调一个阈值要翻 3~4 个文件找硬编码，容易漏改；集中后调参只改一处 |

### 修改

| 文件 | 改动 | 为什么 |
|------|------|--------|
| `indicators.py` | `bb_middle = frame["ma20"]` 替代重复 rolling(20)；窗口期从 config 取 | 原来 `close.rolling(20).mean()` 算了两次，浪费且一旦窗口改了容易只改一处 |
| `data.py` | `_to_float` 返回 `math.nan` 而非 `0.0` | **bug 修复**：原来无效 K 线值变成 0.0 参与 ATR/布林带计算，会让指标失真；NaN 让 pandas 自动跳过 |
| `data.py` | `_extract_trade_date` 加 `tz=UTC+8` | 原来用系统本地时区，部署环境时区不是上海时日期会偏一天 |
| `data.py` | `instrument_type`/`price_precision` 从 config 取 | 原来硬编码 `"etf"`/`3`，如果要支持股票（precision=2）就要改源码 |
| `engine.py` | `_PlanContext` dataclass 替代 dict 传递共享字段 | dict 无类型检查、无 IDE 补全，且 warnings 列表共享引用可能被 append 污染 |
| `engine.py` | `_make_plan` 工厂 + 三个分支函数 | 原来 4 处各写 40+ 行 GridPlan 构造，大量重复字段赋值，改一个字段要改 4 处 |
| `engine.py` | replay 预算一次 `build_indicator_frame`，循环内 `iloc[:i+1]` 切片 | 原来每天重建 DataFrame + 重算所有 rolling，O(n×lookback) 冗余计算 |
| `engine.py` | `0.001` → `10**-precision`；`59` → `cfg.ma_long_window - 1` | 消除残留魔法数，让含义自解释 |
| `regime.py` | slope 阈值、lookback 从 config 取 | 同上：调参不翻源码 |
| `report.py` | `_base_csv_row` 提取公共字段；`_pct_change` 返回 `float|None` | CSV 行 3 处重复 15+ 字段；混合 `float|str` 返回类型让下游无法做数值运算 |
| `report.py` | `_fmt_levels` → `fmt_levels`（公开） | cli.py 有一份重复的 `_fmt_ladder`，统一为一个公开函数 |
| `cli.py` | 删 `_fmt_ladder`，改用 `report.fmt_levels` | 消除重复 |
| `__init__.py` | 导出 `GridConfig`、`DEFAULT_CONFIG` | 外部调用方可以传自定义配置 |

---

## 二、风险披露（行为变更）

| 变更 | 影响范围 | 风险 |
|------|----------|------|
| `_to_float` 返回 NaN 而非 0.0 | 所有经过 `_normalize_rows` 的数据 | 如果上游数据确实有合法的缺失值，现在不会被当作 0 参与计算，指标可能变成 NaN → 进入 disabled 分支。**这是正确行为**，但如果某个 ETF 历史数据有大量缺失值，会比以前更频繁触发 disabled |
| `fmt_levels` 改为公开函数 | `report.py` 的对外接口新增一个符号 | 如果有外部代码 `from atr_grid.report import _fmt_levels` 会 break（不太可能，原来是下划线私有） |
| `_pct_change` 返回 None 而非 `""` | 所有调用 `_pct_change` 的地方 | 已在 report.py 内部用 `_pct_or_blank` 包一层做 CSV 兼容，对外输出无变化 |
| `_disabled_plan` 的 mode 统一为 `"disabled"` | 原来写了一个恒等表达式 `regime.regime if regime.regime == "disabled" else "disabled"` | 行为完全一致，只是代码更清晰 |

---

## 三、验证结果

### 单元测试（66 项全绿）

```
$ python -m pytest tests/atr_grid/ -v
66 passed in 1.01s
```

覆盖范围：
- **纯函数**：quantize_price / _effective_step / _generate_buy_levels / _generate_sell_levels / _suggest_trim_shares / _suggest_tactical_shares / _build_reference_ladder
- **regime 分类**：disabled / trend_up / trend_down / range / 边界（shallow slope）
- **四条路径 smoke**：disabled / trend_up / trend_down / range（含 warnings 不泄漏测试）
- **数据层**：normalize_symbol / _to_float（含 NaN/Inf 边界）
- **报告层**：_pct_change / _pct_abs_change / fmt_levels

### 金标准（replay 数值一致性）

```
$ python -c "from atr_grid.engine import replay_symbol; print(replay_symbol('SH515880', lookback=120))"

{'symbol': 'SH515880', 'lookback': 120, 'buy_hits': 11, 'sell_hits': 22,
 'invalidations': 0, 'breakouts': 0, 'days_grid_enabled': 61, 'data_source': 'api'}
```

### CLI smoke

```
$ python -m atr_grid plan SH515880 --no-save        ✅ trend_trim 输出正常
$ python -m atr_grid replay SH515880 --lookback 30   ✅ 回放统计正常
```

---

## 四、关键参数速查（config.py）

```python
GridConfig(
    # 指标窗口
    ma_short_window=20, ma_long_window=60, bb_window=20, bb_num_std=2.0, atr_window=14,
    # 市场状态判断
    regime_ma_lookback=5, regime_slope_threshold=0.25,
    # 网格步长（占布林带宽的比例）
    step_min_fraction=1/8, step_max_fraction=1/3, grid_level_count=3,
    # 仓位
    reference_position_shares=1000, reference_tranche_shares=100,
    trim_ratio=0.10, tactical_ratio=0.20, lot_size=100,
    # 阶梯档数
    ladder_tranches=3,
)
```

调参只需修改 `config.py` 中的 `DEFAULT_CONFIG`，或在调用时传入自定义 `GridConfig` 实例。

---

## 五、待办（已量化）

| 优先级 | 事项 | 当前状态 | 目标 |
|--------|------|----------|------|
| **紧急** | **更新 xq_a_token**（2026-05-01 过期） | 还剩 7 天 | 浏览器重新导出 Netscape cookie 覆盖原路径 |
| 中 | 添加 logging | 无日志，排查问题靠 print | 关键节点（数据加载失败、regime 判断、plan 模式选择）各一条 INFO/WARNING |
| 中 | GridPlan 字段拆分 | 40+ 字段混合输入/输出 | 拆为 `PlanContext`（~12 字段）+ `PlanResult`（~30 字段），减少认知负担 |
| 低 | replay 更大 lookback 的性能 | lookback=120 约 2~3 秒（已优化为切片） | lookback=500 应 < 10 秒 |

---

## 六、文件结构

```
atr_grid/
├── __init__.py       # 包入口，导出 GridConfig/GridPlan/generate_plan/replay_symbol
├── __main__.py       # python -m atr_grid 入口
├── config.py         # 集中配置
├── cli.py            # CLI 解析与摘要输出
├── data.py           # 行情数据加载与归一化
├── indicators.py     # ATR / 布林带 / 均线计算
├── regime.py         # 市场状态分类（trend_up / trend_down / range）
├── engine.py         # 核心：计划生成 + 回放
└── report.py         # JSON / Markdown / CSV 报告渲染

tests/atr_grid/
├── test_data.py             # normalize_symbol / _to_float
├── test_engine_pure.py      # quantize_price / _effective_step / levels / shares / ladder
├── test_engine_assemble.py  # 四条路径 smoke + warnings 隔离
├── test_regime.py           # classify_regime 全分支
└── test_report.py           # _pct_change / fmt_levels
```
