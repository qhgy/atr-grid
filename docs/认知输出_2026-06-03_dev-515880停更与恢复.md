# 认知输出：`/dev/515880/` 停更与恢复

日期：2026-06-03

## 结论

`https://qhgy.github.io/atr-grid/dev/515880/` 停在 2026-04-27，不是因为页面生成逻辑本身废掉了，而是自动化触发链路断了。

核心原因有两层：

1. `update-515880-dashboard.yml` 只存在于 `dev` 分支，不存在于默认分支 `main`。
2. GitHub Actions 的 `schedule` 定时任务只会读取默认分支上的 workflow 文件，所以 `dev` 分支里的定时配置不会按小时触发。

雪球 Cookie 过期会导致已触发的任务拿不到数据，但这条 `/dev/515880/` 链路的首要问题是定时任务没有被 GitHub 执行。

## 当前恢复动作

本地已使用新的雪球 Cookie 生成最新 `trend_hybrid` 页面：

```powershell
uv run python -m atr_grid multi SH515880 --profile trend_hybrid
```

生成结果：

- 数据来源：`api`
- 最后交易日：`2026-06-03`
- 当前价：`¥1.716`
- 市场状态：`trend_up`
- 当前模式：`trend_trim`
- Hybrid 档位：`high`
- 结论：高位只卖不买，底仓不动，网格层预算为 0

本次只同步发布产物到：

- `docs/dev/515880/index.html`
- `docs/dev/515880/snapshots/2026-06-03.html`

暂不扩大到 `/dev/` 多标的总页，也不改 `/515880/` 主线镜像。

## 后续自动化修复

要让 `/dev/515880/` 后续自动更新，需要把 `update-515880-dashboard.yml` 放到默认分支 `main`，但 workflow 内部仍然 checkout `ref: dev` 来运行开发分支代码。

推荐修复方式：

1. 在 `main` 分支保留一份 `update-515880-dashboard.yml`。
2. workflow 的源码 checkout 固定为 `dev`。
3. 输出仍写回 `main/docs/dev/515880/`。
4. 保留 `workflow_dispatch`，方便手动强制刷新。
5. 如继续使用 proximity gate，需要确认它在 Cookie 失效时是否应该保守放行。

## 验证

本地验证命令：

```powershell
uv run pytest -q
uv run python -m atr_grid multi SH515880 --profile trend_hybrid
```

测试结果：

```text
70 passed
```
