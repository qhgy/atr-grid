name: Update ATR Grid Dashboard

on:
  schedule:
    # 15:30 Beijing time (UTC+8) on weekdays → 07:30 UTC
    - cron: '30 7 * * 1-5'
  workflow_dispatch:
    inputs:
      symbols:
        description: 'ETF symbols space-separated (e.g. SH515880 SH513500)'
        required: false
        default: 'SH515880 SH513500'

permissions:
  contents: write

jobs:
  update-dashboard:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Install dependencies
        run: uv sync

      - name: Generate multi-ETF dashboard
        env:
          XUEQIUTOKEN: ${{ secrets.XUEQIUTOKEN }}
        run: |
          SYMBOLS="${{ github.event.inputs.symbols || 'SH515880 SH513500' }}"
          mkdir -p output
          uv run python -m atr_grid multi $SYMBOLS

      - name: Update docs/index.html
        run: |
          mkdir -p docs
          cp output/atr_grid.html docs/index.html
          echo "Updated: $(date '+%Y-%m-%d %H:%M UTC')" >> docs/index.html

      - name: Commit and push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/index.html
          git diff --staged --quiet || \
            git commit -m "chore: auto-update dashboard $(date '+%Y-%m-%d')" && \
            git push
